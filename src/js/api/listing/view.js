export async function viewListing(id) {}

export async function viewListings(limit = 12, page = 1, tag) {
  // where i will do fetch operation
}

export async function viewListingsByUser(username, limit = 12, page = 1, tag) {}
